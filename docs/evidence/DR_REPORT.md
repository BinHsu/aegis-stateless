# DR drill report — region rebuild

> Generated by `scripts/dr/dr-drill.sh eu-central-1` on 2026-05-17T11:08:44Z.
> Plan + failure-mode matrix: [`../dr-plan.md`](../dr-plan.md).

## Result

| Phase | From → to | Duration |
|---|---|---|
| 0 — baseline | — (2026-05-17T10:48:28Z) | — |
| 1 — teardown (aegis-stateless-eu-central-1 destroyed) | 2026-05-17T10:48:28Z → 2026-05-17T10:57:23Z | 8m 55s |
| 2 — rebuild (Terraform re-apply) | 2026-05-17T10:57:23Z → 2026-05-17T11:08:26Z | 11m 3s |
| 3 — reconverge (ArgoCD syncs from git) | 2026-05-17T11:08:26Z → 2026-05-17T11:08:44Z | 0m 18s |
| **Measured cold-rebuild RTO** (teardown start → workload back) | 2026-05-17T10:57:23Z → 2026-05-17T11:08:44Z | **11m 21s** |

The workload was reconstructed from Terraform state + git with no manual
intervention beyond running this script. Terraform state is the source of
truth; ArgoCD converged the cluster from zero.

## Cross-region failover

While `aegis-stateless-eu-central-1` was torn down and rebuilt, every other enabled region was
probed every 20 s on the greeter health endpoint, through its own ALB:

| Surviving region | Probes | Healthy (200) |
|---|---|---|
| `eu-west-1` | 61 | 61 |

A surviving region serving every probe through the drilled region's
full teardown and rebuild is the redundancy evidence: regional stacks
are independent (separate Terraform state, EKS cluster, ArgoCD), so
losing one does not affect another. DNS-level failover — Route 53
latency records with evaluate-target-health drop the drilled region's
record when its ALB is gone — is verifiable from an operator machine
with `dig @<zone-nameserver> greeter.aegis-stateless.test`, and is evidenced in
[`dns-failover.md`](dns-failover.md).

## Grafana evidence

The live dashboard is not durable evidence — the cluster is torn down after the
demo and Grafana Cloud's free tier retains data only ~14 days. Two screenshots
of the `aegis-greeter — overview` dashboard over the drill window
(2026-05-17T10:48:28Z → 2026-05-17T11:08:44Z) are committed alongside this
report:

- `grafana-dr-curve.png` — the DR curve: the drilled region's metrics drop at
  teardown, flatline through the rebuild, and recover at reconverge.
- `grafana-dr-multi-region.png` — the per-region view (via the `region`
  template variable): eu-central-1 drops while eu-west-1 stays flat.

## CLI log

Failover probe samples: `dr-failover-probe-20260517T104824Z.log` — committed
alongside this report (timestamps + HTTP codes only). The full phase-by-phase
Terraform CLI log (`dr-drill-20260517T104824Z.log`) is kept operator-local — it
carries account-specific ARNs and is gitignored.
