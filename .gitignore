# Local working files (operating notes, drafts, personal backlog — never committed)
CLAUDE.md
backlog.md
_context/

# Project-local toolchain — `make dev-setup` rehydrates ./bin/ from
# scripts/install-tools.sh (pinned versions + SHA256). Never committed.
/bin/

# Go build artifacts
*.exe
*.dll
*.so
*.dylib
*.test
*.out
/greeter
/app/greeter
vendor/

# Terraform
.terraform/
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
!example.tfvars
# Pattern X (multi-region topology as data): repo-root tfvars is the single
# source of truth for var.regions; must be committed.
!regions.auto.tfvars
# .terraform.lock.hcl is intentionally NOT ignored — HashiCorp guidance is to
# commit it per-env so provider versions are pinned + reproducible.
crash.log
crash.*.log

# AWS / secrets
.env
.env.*
*.pem
*.key
.aws/
credentials

# Container / image artifacts
*.tar
*.tar.gz

# IDE / OS
.vscode/
.idea/
*.swp
.DS_Store
